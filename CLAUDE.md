# CLAUDE.md

このリポジトリで作業するClaude Codeへの指示書。

個人用の資産形成ダッシュボードAndroidアプリ。
**何のアプリで、いまどこまで動くかは [README.md](README.md) を読む。**
ここには「どう判断してほしいか」だけを書く。

## 設計の大原則

ここが判断の拠り所。迷ったらこれに照らす。
実装済みのものは、どこでどう実現したかを併記してある。

### Google Driveが正のデータソース
ローカルのRoom DBはあくまで表示用のキャッシュ。失われてもDriveから
再構築できる状態を常に保つ。

> 実装: `backup/` に正規化済みデータを置く(processedの元CSVだけでは
> パーサーを直したときに過去の結果が変わってしまい、復元源にならない)。
> folder IDもキャッシュせず毎回引き直す。
> 詳細は [docs/architecture.md](docs/architecture.md)。

### CSVの投入は雑でよい
銀行から落としたCSVを**ファイル名も気にせず** `inbox` に放り込むだけで、
あとはアプリ側が判定・処理する。ヘッダーが既知フォーマットならフルパース、
未知なら金額だけでも抽出するフォールバックを用意し、
壊れずに動き続けることを優先する。

> 実装: 判定はファイル名ではなく**中身**で行う。ヘッダーの列の顔ぶれで
> アダプターを選び、当たらなければ列の中身から推測する。
> xlsxやPDFを置いてしまっても理由付きで弾く。
> inboxにあるファイルは取り込み済みでも読み直す(戻す動機は
> たいてい「もう一度取り込ませたい」だから)。

### 全自動ではなく、開いたときに処理する
バックグラウンドの定期実行(WorkManager等)には頼らず、アプリを開いた
タイミングで「同期が必要か」を判定して処理する。
ただし「同期してね」という催促通知だけはFCMで能動的に届ける。

### ウィジェットは通知専用
資産額などの数値は表示しない。同期が必要かどうかを色(平常/赤)だけで
示す。詳細はアプリ本体で見る。

### 汎用スキーマ(Metric/Reminder/Goal)で拡張する
NISA・車・家電など専用のテーブルを都度作るのではなく、この3種類の
抽象化に項目として追加していく。

> 実装: 資産推移CSVの`（円）`で終わる列を全部Metricとして拾う。
> 列が増えてもコードを変えずにMetricが1種類増える。
> `投資信託`→NISAのような**対応付けはしない**——metricKeyは列名のまま。
> 表示名や目標との紐付けはE07の仕事で、ここで決め打ちすると後で剥がせない。

## コーディング方針

- 一人用アプリのため、**過度な抽象化や将来の多人数利用を見越した設計は
  避ける**。ただし「銀行が増える」「Goal項目が増える」といった水平方向の
  拡張はしやすくしておく(パーサーのアダプター化、汎用スキーマなど)。
- 失敗時は落ちるより **「処理をスキップしてログに残す」** 方を優先する。
  読めない行が1つあってもファイル全体を捨てない。
  Driveに書けなくても取り込み自体は失敗にしない。
- **壊れるなら見える側に倒す。** 黙って消えるより、おかしな状態のまま
  残って次回気づけるほうがよい(例: 取り込みの記録を先に書いてから
  ファイルを移動する)。
- 個人資産アプリなので**口座番号・氏名・残高の扱いに注意する**。
  生のCSV行はDriveのログにもリポジトリにも書かない。
  logcatに出すのはデバッグビルドだけ。

## ドキュメントの書き分け

| 場所 | 中身 | 正典 |
|------|------|------|
| `CLAUDE.md` | 判断の拠り所、コーディング方針、運用ルール | 大原則 |
| `README.md` | 何のアプリか、どこまで動くか、ビルド手順、フォルダ構成、技術スタック | 事実・数値 |
| `docs/architecture.md` | 全体の流れと、大原則から導いた実装判断 | 横断的な設計 |
| `docs/development.md` | このマシンでの手順、踏んだ落とし穴 | 操作手順 |
| `issues/` | タスクごとの設計判断・検証結果・積み残し | 個別の経緯 |

**同じことを2か所に書かない。** 迷ったら上の「正典」の列に従い、
他の場所からはリンクする。

設計判断の理由は原則としてissuesの各タスクファイルに書く。
複数のタスクにまたがって効くものだけ `docs/architecture.md` に上げる。

## 進捗管理

GitHub Issueは使わない。`issues/epics/`(親)と`issues/tasks/`(子)に
Markdownファイルとして置く。全体像と着手順は
[issues/README.md](issues/README.md) を見る。

- 親イシューは子イシューへのチェックボックス付きリンクを持つ
- 子イシューは親への逆リンクと、概要・完了条件・ステータスを持つ
- タスクに着手/完了したら、該当ファイルの「ステータス」と、親の
  チェックボックスの両方を更新する
- 新しいタスクを思いついたら、まず該当エピックの子イシューとして
  ファイルを追加し、親のチェックボックス一覧にも加える

### タスクを完了とするときに書くこと
「実装した」だけでは後から役に立たない。以下を残す。

- **なぜその作り方にしたか**(特に、他の選択肢を捨てた理由)
- **何をどう検証したか**(実データで確認したのか、テストだけなのか)
- **未検証・未実装のまま残したこと**。完了の体裁を整えるために
  黙って落とさない
