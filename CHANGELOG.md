# 変更履歴

バージョンの付け方は [E08-03](issues/tasks/E08-03-version-history.md)。
番号は `app/build.gradle.kts` の `appVersion` だけを書き換える。

## 2.7.0 — カテゴリの決まりのまとめ取り込み

機能が増えたのでMINORを上げた(E08-03)。

- カテゴリの決まりを、Driveの `settings/category_rules.csv`(言葉,カテゴリ,種類)からまとめて取り込む(E07-22)

## 2.6.0 — 明細のカテゴリと積立投資の目安

機能が増えたのでMINORを上げた(E08-03)。入れたあと、引き継いだ「生活費以外」のうち振替・クレカ積立は、
「振替」「積立投資」に付け直す(そのままだと積立投資の目安が少なめに出る)。

- 明細のカテゴリ(E07-21): 摘要の言葉でカテゴリを付ける。種類(生活費・遊び代・大型出費・振替・積立投資)で、
  生活費・消費・収入への入り方が決まる。前の「生活費から除く言葉」は「生活費以外(引き継ぎ)」として引き継ぐ
- 積立投資の目安(E10-04): (収入 − 消費 − 生活防衛資金・大型出費・積み増しの月額)× 8割を、今の積立額と比べて出す

## 2.5.0 — 生活費から除く出金を登録しやすく

機能が増えたのでMINORを上げた(E08-03)。

- 生活費から除く出金の画面: 摘要ごとに件数・合計・最後に使った日を出し、件数・金額・最近で並べ替えられる。
  タップだけで除く・除かないを切り替え、保存ボタンは下に固定(E07-20)

## 2.4.0 — 目標を満たしているかと月々の額

機能が増えたのでMINORを上げた(E08-03)。

- 目標に「足りないとき何か月で埋めるか」を決められる。決めると詳細に、満たしているかと、足りないときの
  月々の積立額が1行で出る(E07-19)

## 2.3.0 — 系列の色・内訳・口座ごとの残高

機能が増えたのでMINORを上げた(E08-03)。

- 系列ごとに色を分けた。トップの一覧の色の丸・推移の線・純資産の円グラフで同じ色(E03-08)
- リマインダーをトップに並べず、「リマインダー(全N件)」の1行から一覧画面で見る(E05-07)
- 銀行明細の残高から、口座ごとの残高の系列を作る(E02-07)。名前は「名前・表示を変更」で付ける
- 系列に「まとめ先」を選べる。トップでは親の1行にまとめ、親の詳細に内訳として並ぶ(E07-18)

## 2.2.1 — 取り込み直後のキャッシュ

直しただけなのでPATCHを上げた(E08-03)。

- 直したもの: CSVを取り込んだ同期で、書いたばかりのbackupを読めずに「キャッシュ未更新」になることがあった。
  少し待って読み直す(E02-06)

## 2.2.0 — 日付の入力とbackupの不具合

機能が増えたのでMINORを上げた(E08-03)。

- 日付を `20270301` の形で入れられる(E07-17)
- 直したもの: 名前が同じ別のCSVを取り込むと、前のCSVのbackupが上書きされて消えていた。backupの名前に
  元ファイルのIDを入れ、消えたbackup(processedにあるのにbackupの無いファイル)を同期の結果に出す(E01-16)

## 2.1.0 — 大型出費の積立

機能が増えたのでMINORを上げた(E08-03)。

**2.0.0 から上げる前に、2.0.0 で目標・リマインダー・系列を保存しないこと。** 2.0.0 は下の新しい項目を
知らず、保存すると settings から落とす(E07-16)。

- 家電・車などの大型出費の積立(E07-15)。リマインダーに「何年ごと」と積立先を入れると、積立の目標に
  向こう1年に要る額(目標額)と、ならした月々の積立額が出る。物価上昇率を掛けられる
- 大型出費の予定をCSV(Driveの `settings/plan.csv`)からまとめて取り込む(E07-16)

## 2.0.0 — v2(はじめてスマホに入れた版)

1.0.0のあとに増えたもの。v1→v2の区切りなのでMAJORを上げた(E08-03)。
リリース用の鍵で署名し、スマホに入れた(E08-02)。

- 目標(Goal)を追加・編集・削除できる(E07-01)。進捗が一覧と詳細に出る
  - 目標額を「生活費の何か月分」で自動に出せる。振替などは摘要の言葉で除く(E07-06)
  - 届いていなければ、月々いくらで何か月で届くかが出る(E07-07)
  - ふるさと納税のような「毎年1月にリセットする枠」と、使った分を足す記録(E07-09)
  - 期日のある目標に積み増しの時期を決められ、その時期に入ると月額が出て通知が届く(E07-11)
  - 生活防衛資金の下限。期日の目標の不足を下限まで取り崩して補えるかが出る(E07-12)
  - 同じ系列を複数の目標で上から順に分け、推移を目標ごとに色分けした積み上げグラフで見る(E07-10・E07-13)
- 系列の表示名の変更と、一覧から隠す(E07-14)。iDeCoなどを手入力の系列として持てる(E07-05)
- 通知を端末の中で出す: 同期の催促・リマインダー・目標の不足など。リマインダーを登録できる(E05)
- 今のペースならいつ目標に届くか(E09-01)、生活防衛資金が何か月分あっていつ目標を割るか(E09-02)、
  向こう3年の大型出費の予定(E09-04)
- アプリを生体認証か画面ロックで守る(E06-01〜03)。金額の見せ方を実額/%/マスクで切り替えられる(E06-04)
- カードの利用明細を取り込み、銀行のカード引き落としと二重に数えない(E01-14)
- 証券口座の保有商品一覧を、区分ごとの評価額として取り込む(E01-15)
- 純資産: 選んだ系列の合計の推移をトップの一番上に出す(E10-01)。系列ごとの配分を円グラフで見る(E10-03)
- 余剰資金(ボーナスなど)の額を入れると、どの目標にいくら回すかの案が出る(E10-02)

直したもの:
- 小数のある額(`1234567.00` など)を100倍に読んでいた(E01-15)

## 1.0.0 — v1(まだスマホには入れていない)

最初の目標(E00〜E04)が揃った版。

- CSVを Drive の inbox に置いて同期すると、取り込んで `backup` に正規化した形で残す(E01)
- 同期のたびに Drive の中身からローカルDBを作り直す。入れ直しても同期すれば戻る(E02)
- 手元のデータが30日より古ければ、開いたときに自動で同期する(E02-04)
- トップに項目の一覧、詳細に推移の折れ線・増減の棒・月次の表(E03-01, 02, 06)
- 月次・年次サマリー(E03-03)
- 値の補正・手入力。Drive の `corrections` に保存(E03-04)
- 前回の同期の要約と詳細(E03-05)
- AI相談用の書き出し(E03-07)
- 同期が必要になると赤くなるホーム画面ウィジェット(E04)
