# CLAUDE.md

このファイルは、このリポジトリで作業するClaude Codeへの指示書。

## プロジェクト概要

個人用の資産形成ダッシュボードAndroidアプリ。NISA運用状況、車購入用貯金、
緊急予備資金、車のメンテナンス予定、銀行口座残高などを一元管理し、
Androidのホームウィジェットで「何か対応が必要か」を一目で確認できる
ようにする。マネーフォワード等の外部サービスへの課金を避け、
自分専用にカスタマイズできる形で自作する。

## 設計の大原則

- **Google Driveが正のデータソース**。ローカルのRoom DBはあくまで
  表示用のキャッシュであり、失われてもDriveから再構築できる状態を
  常に保つ。
- **CSVの投入は雑でよい**。銀行から落としたCSVをファイル名も気にせず
  `inbox`フォルダに放り込むだけで、あとはアプリ側が判定・処理する。
  ヘッダーが既知フォーマットならフルパース、未知なら金額だけでも
  抽出するフォールバックを用意し、壊れずに動き続けることを優先する。
- **全自動ではなく、開いたときに処理する**。バックグラウンドの定期
  実行(WorkManager等)には頼らず、アプリを開いたタイミングで
  「同期が必要か」を判定して処理する。ただし「同期してね」という
  催促通知だけはFCMで能動的に届ける。
- **ウィジェットは通知専用**。資産額などの数値は表示せず、同期が
  必要かどうかを色(平常/赤)だけで示す。詳細はアプリ本体で見る。
- **汎用スキーマ(Metric/Reminder/Goal)で拡張する**。NISA・車・家電
  など専用のテーブルを都度作るのではなく、この3種類の抽象化に
  項目として追加していく。

## Driveのフォルダ構成

```
/資産アプリ/
  /inbox/       ← CSVをここに放り込むだけ(人間が触るのはここだけ)
  /processed/   ← 取り込み済みCSVの移動先(アプリが管理)
  /backup/      ← 整形済みデータのバックアップ(アプリが書き込む)
  /corrections/ ← 手動補正データ(アプリが書き込む)
  /logs/        ← 取り込み失敗時のエラーログ(アプリが書き込む)
```

## 技術スタック

- Kotlin + Jetpack Compose
- Glance(Androidウィジェット)
- Room DB(ローカルキャッシュ)
- Google Drive API(OAuth 2.0)
- Firebase Cloud Messaging(通知)
- ストア非公開、署名付きAPKを直接配布(個人利用のみ)

## ドキュメントの置き場所

| 場所 | 中身 |
|------|------|
| `README.md` | 何のアプリか、いまどこまで動くか、ビルド手順 |
| `docs/architecture.md` | 仕組みと「なぜそうしたか」 |
| `docs/development.md` | このマシンでの開発手順、踏んだ落とし穴 |
| `issues/` | タスクごとの設計判断と検証結果 |

設計判断の**理由**はissuesの各タスクファイルに書く。
横断的に効く判断だけ `docs/architecture.md` に集約する。
同じことを2か所に書かない。

## 進捗管理

GitHub Issueは使わない。`issues/epics/`(親)と`issues/tasks/`(子)に
Markdownファイルとして置く。全体像と着手順は `issues/README.md` を見る。

- 親イシューは子イシューへのチェックボックス付きリンクを持つ
- 子イシューは親への逆リンクと、概要・完了条件・ステータスを持つ
- タスクに着手/完了したら、該当ファイルの「ステータス」と、親の
  チェックボックスの両方を更新する
- 新しいタスクを思いついたら、まず該当エピックの子イシューとして
  ファイルを追加し、親のチェックボックス一覧にも加える

## 他の個人プロジェクトとの関係

- RepositoryAutomation(ローカルLLM経由のIssue→PR自動生成)とは無関係。
  このプロジェクトのコードは全てClaude(このセッション)に書かせる。
- 既存のNISAトレーリング通知(PC側で日次スクレイピング)は、この
  アプリの外で動き続ける。E05-04でFCM経由の通知転送だけを繋ぐ。

## コーディング方針

- 一人用アプリのため、過度な抽象化や将来の多人数利用を見越した
  設計は避ける。ただし「銀行が増える」「Goal項目が増える」といった
  水平方向の拡張はしやすくしておく(パーサーのアダプター化、
  Metric/Reminder/Goalの汎用スキーマなど)。
- 失敗時は落ちるより「処理をスキップしてログに残す」方を優先する
  (E01のパーサー、E06のオフライン処理など)。
