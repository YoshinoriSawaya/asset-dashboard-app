# E02-03: Drive取り込み結果→ローカルキャッシュ反映の同期処理
親: [E02: ローカルデータモデル・キャッシュ基盤](../epics/E02-local-cache-and-model.md)

## 概要
E01で取り込んだCSVのパース結果を、E02-02のRoom DBに書き込む処理。
corrections(E01-10)も合わせて反映する。

## 完了条件
CSV取り込みが成功すると、対応するMetric/Goalの値がローカルDBに
反映される。

## 着手前に必要なこと
E01-07の時点で3ファイルを取り込み済みとして記録し、processedへ移動した。
**中身はどこにも保存していない**(保存先のスキーマがこのエピックのため)。

DB保存を実装したら、processedのファイルを読み直すこと。
Driveでinboxへ戻すか、`ingested_file` テーブルを空にすれば再取り込みされる。
詳細は [E01-07](E01-07-move-to-processed.md) の「E02に持ち越す注意」。

## ステータス
未着手
